services:
  - type: web
    name: border-crossing-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "app:create_app()"
    disk:
      name: sqlite-data
      mountPath: /data
      sizeGB: 1
